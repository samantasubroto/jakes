import { IconConfig, IconConfigResource } from "@spartacus/storefront";

export const jakesIconConfig: IconConfig = {
  icon: {
    symbols: {
      SIGNOUT: 'fas fa-sign-out-alt',
      LIKE: 'fas fa-heart',
      UNLIKE: 'far fa-heart',      
    }
  }
}